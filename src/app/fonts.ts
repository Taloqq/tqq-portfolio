import { Inter, Ysabeau } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-sans',
  weight: '500'
});

export const ysabeau = Ysabeau({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
})